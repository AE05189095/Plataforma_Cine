# Ejemplo de variables de entorno para el servidor
# Copia este archivo a ".env" y rellena los valores en tu entorno local o en el servidor de producción.

# Cadena de conexión a MongoDB (obligatoria)
MONGODB_URI=mongodb://usuario:password@host:puerto/nombre_basedatos

# Clave secreta para firmar JWT (obligatoria)
JWT_SECRET=una_clave_muy_segura_y_larga

# Origen permitido para CORS. En desarrollo puede usarse '*' o 'http://localhost:3000'
# En producción, establece el dominio exacto (ej: https://misitio.com)
ALLOWED_ORIGIN=http://localhost:3000

# Puerto donde el servidor Express escuchará (opcional)
PORT=5000

# Notas:
# - Nunca subas el archivo .env con valores reales al repositorio.
# - Para producción, asegúrate de usar secretos fuertes y restringir ALLOWED_ORIGIN.
# - Considera almacenar secretos en un gestor (Vault, AWS Secrets Manager, Azure Key Vault, etc.).
